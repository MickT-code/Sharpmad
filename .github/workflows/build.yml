name: Build SharpMad Workflow 21

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:   # handmatig starten altijd mogelijk

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup MSBuild
      uses: microsoft/setup-msbuild@v2

    - name: Restore NuGet packages (if any)
      shell: pwsh
      run: |
        dotnet restore Sharpmad.sln

    - name: Build Sharpmad (Release)
      shell: pwsh
      run: |
        msbuild Sharpmad.sln /p:Configuration=Release

    - name: Upload EXE artifact
      uses: actions/upload-artifact@v4
      with:
        name: Sharpmad-Release
        path: |
          bin/**/*.exe   # pakt alle EXE's in bin en subfolders
